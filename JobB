pipeline {
    agent any
    stages {
        stage('test') {
            steps {
                echo 'Building the application...'
            }
        }
    }
    post {
        success {
            // Trigger JobC, but do not fail this pipeline if JobC fails
            build job: 'JobC', propagate: false
        }
    }
}
