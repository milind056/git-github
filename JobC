pipeline {
    agent any
    stages {
        stage('Deploy') {
            steps {
                echo 'Deploying application...'
            }
        }
    }

 post {
        always {
            // Clears the workspace after the build
            cleanWs()
        }
    }
}
